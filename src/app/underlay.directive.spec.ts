import { UnderlayDirective } from './underlay.directive';

describe('UnderlayDirective', () => {
  it('should create an instance', () => {
    const directive = new UnderlayDirective();
    expect(directive).toBeTruthy();
  });
});
